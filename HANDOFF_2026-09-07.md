# HANDOFF — GDI platform (gdiconsult.online) — 2026-09-07

Start here in a new chat. Memory files: `project_gdi_platform.md`, `reference_vps2.md` (creds), `MEMORY.md` index.

## 1. Where things are

| What | Where |
|---|---|
| Code | GitHub `bandurkas/GDI`, branch `main`, local clone `~/dev/GDI` (push via `git -c credential.helper='!gh auth git-credential' push origin main`) |
| Production | VPS2 `168.231.118.173`, `/var/www/gdi-platform`, pm2 `gdi-platform` (`next start -p 3003`), PostgreSQL db `gdi_platform` (role `styserg`, peer auth) |
| Nginx | `/etc/nginx/sites-enabled/gdiconsult` (gdiconsult.online + www, certbot cert to 2026-12-06), `gdi-alias` (preview `https://gdi.168-231-118-173.sslip.io`, noindex) |
| Deploy | `cd /var/www/gdi-platform && git pull && npx prisma generate && npx prisma db push && NODE_OPTIONS=--max-old-space-size=1536 npm run build && pm2 restart gdi-platform` (also in `DEPLOY.md`) |
| Env | `/var/www/gdi-platform/.env` — DATABASE_URL, NEXTAUTH_*, BANK_* (placeholders!), ONLINE_PAYMENT_ENABLED=false, NEXT_PUBLIC_USD_IDR_RATE=17600 |
| Admin | bandurkas@gmail.com (password given in chat 07.09; reset via bcrypt+prisma script if lost) |
| Tests | `DATABASE_URL=postgresql://x@localhost:5432/x npx vitest run` → 45 passing; `npx tsc --noEmit` clean |

Last commit deployed: see `git log -1` (07.09 evening: value-based ops pricing + card polish).

## 2. What was done on 2026-09-07 (chronological)

1. Site restored from GitHub onto VPS2 (VPS1 terminated). Domain A-record moved by user to VPS2; certbot issued.
2. Midtrans online payment disabled → **bank transfer** flow (`PaymentMethod.BANK_TRANSFER`, order PENDING until admin confirms in Orders tab; cashback credited on confirm). `b1e69ca`.
3. **Managed Server Colocation & AI Infrastructure** section `/services/managed-colocation` per spec `GDI_MANAGED_COLOCATION_SECTION.md` (user's doc): 17 sections, calculator 1–200 servers, presets 1U/2U/Storage/L40S/H100/H200/B200/B300/custom, B300 priced at 15 kW, enterprise tiers, lead form → `ColocationLead` table + admin **Leads** tab, analytics bridge, EN/ID. `588facf`, `7591b0a`.
4. **Home page rewritten**: honest "AI + infrastructure" positioning, no payment mockup / fake logos, "Two ways we help" section, colocation links in nav/footer. `5eaafa9`.
5. **Managed Server Operations** (OS/app-level management of customer servers) added to colocation page + calculator step 7 + lead field. `9496dbc`.
6. IDR never abbreviated ("M" = miliar in Indonesia) → full figures + "≈ $" hints (rate via env). `c13aaaa`.
7. **Ops pricing reworked** (user decision): price = (labour basis + hardware-responsibility reserve = hardware value × 5%/yr Standard or 10%/yr Advanced & GPU) × **1.30**; onboarding includes connection, initial config and customer software install (2/6/10/24 h). Price matrix by server class on page; custom-server hardware value input. `06eefbc`. Basis in `docs/PRICING_SERVER_OPS.md` (internal).

8. **Domain fix (evening)**: gdiconsult.online had a stale **AAAA** record (2a02:4780:59:c3d3::1, old host) → IPv6 clients timed out while IPv4 worked. User deleted it in Hostinger; nginx `gdiconsult` now also listens on `[::]:443` / `[::]:80` (VPS2 IPv6 = `2a02:4780:59:f46c::1`, backup of old config in `/root/gdiconsult.nginx.bak-*`). Hostinger's authoritative NS then served an auto-generated private `fd00:aa:bb:2050::a8e7:76ad` AAAA; user was asked to add AAAA `@` → `2a02:4780:59:f46c::1` (not yet visible on ns1/ns2 at handoff time — re-check with `dig AAAA gdiconsult.online @ns1.dns-parking.com`).
9. **/products rebuilt as "AI Services"** (`582ff9d`, `72a74b3`): 4 fixed-scope packages with **new names and market prices** (owner decision) — AI Readiness Sprint Rp 19.5M / 7 wd, Automation Pilot Rp 45M / 15 wd, Automation Program Rp 120M / 30–40 wd, Private AI Assistant Rp 150M / 20–30 wd. Product ids unchanged (`start-ai`, `middle-scale`, `automation-platform`, `ent-assistant`) so cart/orders keep working; `/api/seed` now reads names/prices from `src/lib/services/config.ts` (run on prod → DB updated). Page: hero ladder, package cards (BuyButton → existing cart/bank-transfer flow), 3-question package finder, full scope per package, comparison table, process, "beyond packages" + **request form → `ServiceLead` table + admin tab "Service Requests"** (`/api/leads/services`, `/api/admin/service-leads/[id]`), stack, illustrative industry use cases, FAQ. Copy EN/ID in `src/lib/services/content.ts`; pricing basis in `docs/PRICING_AI_SERVICES.md` (internal). Removed: ProductDrawer (Stripe / money-back copy), fabricated cases, "99.99% uptime". Cashback is intentionally **not** mentioned on the page (owner decision); the cashback mechanism itself (cart/dashboard/admin) is untouched. Navbar label "Products" → "AI Services" / "Layanan AI".

## 3. Key design rules (do not break)

- GDI is a **multi-data-center managed colocation provider**, never a data-center owner; no partner names/certs; no fake stats/logos.
- All colocation prices/presets live in `src/lib/colocation/config.ts`; copy EN/ID in `content.ts`; pure calc in `calc.ts` (+tests). Markup is never shown publicly.
- Monthly price does **not** change with contract term (spec §15). User asked about it; no term discounts introduced yet.
- Calculator "from" prices on home/colocation page are computed from config, never hardcoded.
- Existing site design system (Inter, indigo/slate, rounded-3xl cards) reused; colocation section uses "engineering datasheet" accents (mono figures, blueprint grid, rack SVG viz).

## 4. Open items / next steps

- [ ] **Real bank requisites** in `.env` (BANK_NAME, BANK_ACCOUNT_NUMBER, BANK_ACCOUNT_HOLDER, BANK_SWIFT) — still placeholders; restart pm2 after.
- [ ] User to review copy (EN + ID) and prices on `/services/managed-colocation` and home.
- [ ] Decide: optional cheaper "GPU ops without hardware reserve" plan (~Rp 9.75M/node) — offered, not built.
- [ ] Decide: term discounts (6/12/24 mo) — not built.
- [ ] Confirm Hostinger AAAA `@` = `2a02:4780:59:f46c::1` is live on ns1/ns2.dns-parking.com; then verify `curl -6 https://gdiconsult.online`.
- [ ] User to review the new `/products` copy (EN + ID) and the four package prices; decide whether the cashback mechanism (cart/dashboard) should be removed entirely.
- [ ] Footer phone `+62 123 4567 890` is a placeholder; Twitter/Instagram links are `#`.
- [ ] Remove preview alias `gdi-alias` (nginx + cert) once user confirms gdiconsult.online resolves for them.
- [ ] USD hint rate is static (env). Optional: daily FX fetch.
- [ ] Branch `feat/forgot-password-and-auth-fixes` unmerged (pre-existing).
- [ ] VPS2 pm2 `gdi-staging` (FutureWorks staging, other project) is in a restart loop — not touched.

## 5. Quick verification

```bash
curl -s -o /dev/null -w "%{http_code}\n" https://gdiconsult.online/services/managed-colocation   # 200
# calculator smoke (Playwright script pattern used today lives only in the session scratchpad; re-create if needed)
```
Expected calculator checks: 1U×3 + Standard ops = Rp 10,080,000? **No — now Rp 12,240,000** (3×1.56M + 3×2.52M); B200 + GPU ops = Rp 114,540,000/mo, setup Rp 17,700,000; term change never alters monthly.
